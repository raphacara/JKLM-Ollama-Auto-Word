# Setup Notes

The main project documentation is now in `README.md`.

Use:

- `README.md` for public/GitHub documentation
- `jklm_settings.example.json` as the public config template
- `jklm_settings.json` only for your local machine
